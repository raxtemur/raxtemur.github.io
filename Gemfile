source "https://rubygems.org"

# Use the GitHub Pages gem to match GitHub's build environment locally
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"


